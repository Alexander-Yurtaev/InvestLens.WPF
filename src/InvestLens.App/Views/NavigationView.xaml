<UserControl x:Class="InvestLens.App.Views.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             d:DataContext="{d:DesignInstance viewModel:NavigationViewModel, IsDesignTimeCreatable=True}"
             xmlns:viewModel="clr-namespace:InvestLens.ViewModel;assembly=InvestLens.ViewModel"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:behaviors="clr-namespace:InvestLens.App.Behaviors"
             xmlns:local="clr-namespace:InvestLens.App.UserControls"
             xmlns:navigationTree="clr-namespace:InvestLens.ViewModel.NavigationTree;assembly=InvestLens.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="320">

    <UserControl.Resources>
        <SolidColorBrush x:Key="IsMouseOverBackgroundBrush" Color="{StaticResource BrandRed15Color}"/>
        <SolidColorBrush x:Key="IsCheckedBackgroundBrush" Color="{StaticResource BrandRed25Color}"/>
        <SolidColorBrush x:Key="GlyphBrush" Color="#CDD9F0" />
        <Style x:Key="ExpandCollapseToggleStyle" TargetType="{x:Type ToggleButton}">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Grid Width="15" 
                              Height="13" 
                              Background="Transparent">
                            <Path x:Name="ExpandPath" 
                                  HorizontalAlignment="Left" 
                                  VerticalAlignment="Center" 
                                  Margin="1,1,1,1" 
                                  Fill="{StaticResource GlyphBrush}"
                                  Data="M 4 0 L 8 4 L 4 8 Z"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Data" 
                                        TargetName="ExpandPath" 
                                        Value="M 0 4 L 8 4 L 4 8 Z"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type TreeViewItem}">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="IsExpanded" Value="True"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TreeViewItem}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition MinWidth="19" Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" x:Name="Bd" Background="Transparent"/>
                            <ToggleButton Grid.Column="0" Grid.Row="0" x:Name="Expander" ClickMode="Press" IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource Mode=TemplatedParent}}" Style="{StaticResource ExpandCollapseToggleStyle}"/>
                            <Border Grid.Row="0" Background="Transparent" BorderBrush="Transparent" BorderThickness="0" Grid.Column="1" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                                <ContentPresenter x:Name="PART_Header" ContentSource="Header" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </Border>
                            <ItemsPresenter x:Name="ItemsHost" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="1"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="false">
                                <Setter Property="Visibility" TargetName="ItemsHost" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="HasItems" Value="false">
                                <Setter Property="Visibility" TargetName="Expander" Value="Hidden"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter Property="Background" TargetName="Bd" Value="{StaticResource IsCheckedBackgroundBrush}"/>
                                <!--<Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>-->
                            </Trigger>
                            <!--<MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="true"/>
                                    <Condition Property="IsSelectionActive" Value="false"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.InactiveSelectionHighlightBrushKey}}"/>
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}}"/>
                            </MultiTrigger>-->
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type ToggleButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="12, 10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border CornerRadius="10"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource IsMouseOverBackgroundBrush}"/>
                </Trigger>
                <!--<Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="{StaticResource BrandRed25Color}"/>
                </Trigger>-->
            </Style.Triggers>
        </Style>
        <DataTemplate DataType="{x:Type navigationTree:NavigationTreeDivider}">
            <Border BorderThickness="1"
                    BorderBrush="#14FFFFFF"
                    Background="#14FFFFFF"
                    Margin="0, 12"
                    Width="235"
                    HorizontalAlignment="Stretch"/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type navigationTree:NavigationTreeItem}">
            <ToggleButton Foreground="{StaticResource BackgroundSecondaryBrush}"
                          HorizontalContentAlignment="Left"
                          Cursor="Hand"
                          Width="200"
                          BorderThickness="0"
                          Command="{Binding NavigateCommand}"
                          CommandParameter="{Binding}"
                          IsChecked="{Binding RelativeSource={RelativeSource AncestorType=TreeViewItem}, Mode=TwoWay, Path=IsSelected}"
                          Checked="ToggleButton_OnChecked">
                <ToggleButton.Resources>
                    <Style TargetType="{x:Type TextBlock}">
                        <Setter Property="Foreground" Value="#CDD9F0"/>
                        <Setter Property="FontWeight" Value="Medium"/>
                        <Setter Property="Margin" Value="0, 0, 12, 0"/>
                    </Style>
                </ToggleButton.Resources>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Icon}"
                       Width="24"
                       FontSize="18"/>
                    <TextBlock Text="{Binding Header}"
                       FontSize="14"
                       VerticalAlignment="Center"/>
                </StackPanel>
            </ToggleButton>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Background="{StaticResource BrandBlueBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Padding="24, 14"
                Margin="0, 0, 0, 16"
                BorderThickness="0, 0, 0, 1"
                BorderBrush="#19FFFFFF">
            <local:Logo/>
        </Border>
        <Border Grid.Row="1"
                Padding="12, 0, 12, 24">
            <TreeView ItemsSource="{Binding MenuItems}" 
                      Background="Transparent">
                <b:Interaction.Behaviors>
                    <behaviors:SelectFirstTreeItemBehavior/>
                    <behaviors:SelectedItemBehavior SelectedItem="{Binding SelectedItem}"/>
                </b:Interaction.Behaviors>
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate DataType="{x:Type navigationTree:NavigationTreeItem}" 
                                              ItemsSource="{Binding Children}">
                        <ContentPresenter Content="{Binding}" />
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </Border>
    </Grid>
</UserControl>
