<UserControl x:Class="InvestLens.App.UserControls.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             d:DataContext="{d:DesignInstance viewModel:NavigationViewModel, IsDesignTimeCreatable=True}"
             xmlns:menu="clr-namespace:InvestLens.Model.Menu;assembly=InvestLens.Model"
             xmlns:viewModel="clr-namespace:InvestLens.ViewModel;assembly=InvestLens.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="320">

    <UserControl.Resources>
        <SolidColorBrush x:Key="IsMouseOverBackgroundBrush" Color="#26C8102E"/>
        <SolidColorBrush x:Key="IsCheckedBackgroundBrush" Color="#40C8102E"/>
        <SolidColorBrush x:Key="GlyphBrush" Color="#CDD9F0" />
        <Style x:Key="ExpandCollapseToggleStyle" TargetType="ToggleButton">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid Width="15" 
                              Height="13" 
                              Background="Transparent">
                            <Path x:Name="ExpandPath" 
                                  HorizontalAlignment="Left" 
                                  VerticalAlignment="Center" 
                                  Margin="1,1,1,1" 
                                  Fill="{StaticResource GlyphBrush}"
                                  Data="M 4 0 L 8 4 L 4 8 Z"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Data" 
                                        TargetName="ExpandPath" 
                                        Value="M 0 4 L 8 4 L 4 8 Z"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TreeViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="IsExpanded" Value="True"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TreeViewItem}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition MinWidth="19" Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" x:Name="Bd" Background="Transparent"/>
                            <ToggleButton Grid.Column="0" Grid.Row="0" x:Name="Expander" ClickMode="Press" IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource Mode=TemplatedParent}}" Style="{StaticResource ExpandCollapseToggleStyle}"/>
                            <Border Grid.Row="0" Background="Transparent" BorderBrush="Transparent" BorderThickness="0" Grid.Column="1" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                                <ContentPresenter x:Name="PART_Header" ContentSource="Header" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </Border>
                            <ItemsPresenter x:Name="ItemsHost" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="1"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="false">
                                <Setter Property="Visibility" TargetName="ItemsHost" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="HasItems" Value="false">
                                <Setter Property="Visibility" TargetName="Expander" Value="Hidden"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter Property="Background" TargetName="Bd" Value="{StaticResource IsCheckedBackgroundBrush}"/>
                                <!--<Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>-->
                            </Trigger>
                            <!--<MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="true"/>
                                    <Condition Property="IsSelectionActive" Value="false"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.InactiveSelectionHighlightBrushKey}}"/>
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}}"/>
                            </MultiTrigger>-->
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type ToggleButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="12, 10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border CornerRadius="10"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource IsMouseOverBackgroundBrush}"/>
                </Trigger>
                <!--<Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="#40C8102E"/>
                </Trigger>-->
            </Style.Triggers>
        </Style>
        <DataTemplate DataType="{x:Type menu:MenuDivider}">
            <Border BorderThickness="1"
                    BorderBrush="#14FFFFFF"
                    Background="#14FFFFFF"
                    Margin="0, 12"
                    Width="235"
                    HorizontalAlignment="Stretch"/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type menu:MenuNode}">
            <ToggleButton 
                Foreground="White"
                HorizontalContentAlignment="Left"
                Cursor="Hand"
                Width="200"
                BorderThickness="0"
                Command="{Binding RelativeSource={RelativeSource AncestorType=TreeView}, Path=DataContext.NavigateCommand}"
                CommandParameter="{Binding}"
                IsChecked="{Binding RelativeSource={RelativeSource AncestorType=TreeViewItem}, Mode=TwoWay, Path=IsSelected}">
                <ToggleButton.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="#CDD9F0"/>
                        <Setter Property="FontWeight" Value="Medium"/>
                        <Setter Property="Margin" Value="0, 0, 12, 0"/>
                    </Style>
                </ToggleButton.Resources>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Icon}"
                       Width="24"
                       FontSize="18"/>
                    <TextBlock Text="{Binding Header}"
                       FontSize="14"
                       VerticalAlignment="Center"/>
                </StackPanel>
            </ToggleButton>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Background="{StaticResource MenuBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Padding="24, 14"
                Margin="0, 0, 0, 16"
                BorderThickness="0, 0, 0, 1"
                BorderBrush="#19FFFFFF">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Border Width="36"
                        Height="36"
                        Background="#C8102E"
                        CornerRadius="10">
                    <TextBlock FontWeight="Bold"
                               FontSize="18"
                               Foreground="White"
                               Margin="6"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center">IL</TextBlock>
                </Border>
                <TextBlock FontSize="20"
                           FontWeight="SemiBold"
                           Margin="12"
                           Foreground="White">Invest Lens</TextBlock>
            </StackPanel>
        </Border>
        <Border Grid.Row="1"
                Padding="12, 0, 12, 24">
            <TreeView ItemsSource="{Binding MenuItems}" 
                      Background="Transparent">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate DataType="{x:Type menu:MenuNode}" 
                                              ItemsSource="{Binding Children}">
                        <ContentPresenter Content="{Binding}" />
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </Border>
    </Grid>
</UserControl>
