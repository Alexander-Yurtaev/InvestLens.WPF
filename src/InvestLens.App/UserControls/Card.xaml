<UserControl x:Class="InvestLens.App.UserControls.Card"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converters="clr-namespace:InvestLens.App.Converters"
             xmlns:wrappers="clr-namespace:InvestLens.ViewModel.Wrappers;assembly=InvestLens.ViewModel"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance wrappers:CardWrapper, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800">

    <Border Style="{StaticResource CardBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Margin="0 0 0 16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" 
                           Text="{Binding Title}"
                           FontSize="18"
                           Foreground="{StaticResource BrandBlue}"
                           FontWeight="Bold"
                           TextWrapping="WrapWithOverflow"/>
                <Border Grid.Column="1"
                        Background="{Binding CardTypeBackground, Converter={x:Static converters:StringToSolidColorBrushConverter.Instance}}"
                        Padding="10 4"
                        CornerRadius="20">
                    <TextBlock Text="{Binding CardType}"
                               Foreground="{Binding CardTypeForeground, Converter={x:Static converters:StringToSolidColorBrushConverter.Instance}}"
                               FontSize="11"
                               FontWeight="Medium"/>
                </Border>
            </Grid>
            <Grid Grid.Row="1">
                <Border Margin="0 0 0 16"
                        Padding="0 12"
                        BorderThickness="0 1 0 1"
                        BorderBrush="#E9EDF2">
                    <ItemsControl ItemsSource="{Binding Stats}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type wrappers:StatWrapper}">
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{Binding Title}" 
                                               FontSize="11"
                                               Foreground="#5A6E8A"
                                               Margin="12 0"/>
                                    <TextBlock Text="{Binding ValueDisplay}"
                                               FontSize="18"
                                               FontWeight="Bold"
                                               Margin="12 0"
                                               Foreground="{Binding ValueDisplay, Converter={x:Static converters:ValueDisplayToForegroundConverter.Instance}, ConverterParameter=#002855}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Border>
            </Grid>
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" 
                           FontSize="12"
                           Foreground="#8E92A8"
                           Text="{Binding LastDateUpdate}"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
